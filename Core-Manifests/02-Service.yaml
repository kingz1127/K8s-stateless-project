apiVersion: v1
kind: Service
metadata:
  name: nginx-service-example
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    nodePort: 32563
  selector:
    app: nginx
    environment: test
